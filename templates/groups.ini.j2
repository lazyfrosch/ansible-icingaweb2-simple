; {{ ansible_managed }}

[icingaweb2]
backend = "db"
resource = "icingaweb_db"

{% for item in icingaweb2_groups %}
[{{ item.name }}]
{% for key, value in item.items() %}
{%- if key != 'name' %}
{{ key }} = "{{ value }}"
{% endif %}
{% endfor %}
{% endfor %}
